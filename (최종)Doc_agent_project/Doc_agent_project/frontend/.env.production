# Production environment variables
# Backend API URL - update this to match your deployment
# Use localhost when running backend in Docker but accessing from host browser
VITE_FASTAPI_URL=http://localhost:8000
